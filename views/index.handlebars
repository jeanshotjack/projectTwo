<div>

  {{!-- Side Bar --}}
  <div class="sidebar d-none">

    <div class="card">
      <div class="card-body">
        {{!-- This user's username --}}
        <h6 class="card-subtitle mb-2">@{{user.username}}</h6>
        <a href="/">
          <h1 class="card-title" id="sidebar_logo_text">NOX</h1>
        </a>
        {{!-- These should be editable on click and updated in the database --}}
        {{!-- This user's pronouns --}}
        {{!-- This user's ig username --}}
        <p class="card-text">Pronouns: {{user.pronouns}}</br>Insta: {{user.insta}}</p>
      </div>
    </div>

    <div class="row mt-3">

      <div class="col">

        {{!-- New Post Button --}}
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#newPostModal"
          data-whatever="">New
          Post</button>

      </div>

    </div>

    <div class="row mt-3">

      <div class="col" id="sidebar_nav">
        {{!-- 
        <button type="button" class="btn btn-link" href="/rules">Rules</button> --}}
        <a class="btn" href="/rules"> Rules</a>
        {{!-- <button type="button" class="btn btn-link" href="/signout">Sign Out</button> --}}
        <a class="btn" href="/signout">Sign Out</a>

      </div>

    </div>

  </div>

  {{!-- NEW Side Bar --}}
  <div class="sidebar">

    {{!-- Card for Logo and User Info --}}
    <div class="row">
      <div class="col">

        <div class="pt-2 pb-2 pl-4 pr-4">
          <a href="/"><img class="card-img-top" src="../assets/images/nox-logo.png"
              alt="Social and Dating App for Marginalized Genders"></a>
        </div>

        <p class="user_info">
          <h5 class="card-title bolder-text mb-0">{{user.username}}</h5>
        </p>

        {{!-- Pronouns: "Pronouns Here" --}}
        <p class="user_info"><small text="text-muted">My Pronouns:</small><br />{{user.pronouns}}</p>

        {{!-- IG Handle: "Handle_Here" --}}
        <p class="user_info"><small text="text-muted">My IG Handle:</small><br />{{user.insta}}</p>


      </div>
    </div>

    {{!-- New Post Form --}}
    <div class="row mt-3">
      <div class="col">

        <div class="form-group">
          <label for="post-text" class="bolder-text">New Post</label>
          <input type="text" id="post-text" class="form-control" aria-describedby="post-text" placeholder="*Title*">
        </div>

        <div class="form-group">
          {{!-- <label for="post-description">Post</label> --}}
          <textarea class="form-control" id="post-description" aria-describedby="post-description"
            placeholder="*Feelings*"></textarea>
        </div>

        <button id="submit" class="btn btn-primary" data-id={{user.id}}>Submit</button>

      </div>
    </div>

    {{!-- New Post Button --}}
    <div class="row mt-3 d-none">
      <div class="col">

        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#newPostModal"
          data-whatever="">New
          Post</button>

      </div>
    </div>

    {{!-- Rules and Sign Out Links --}}
    <div class="row mt-3">
      <div class="col" id="sidebar_nav">

        {{!-- <a href="/rules" class="btn btn-link">Rules</a> --}}

        <a href="/signout" class="btn btn-link">Sign Out</a>

      </div>
    </div>

  </div>

  {{!-- Main Content Div --}}
  <div class="content">

    {{!-- Form Row --}}
    <div class="row d-none">

      <div class="col col-12">

        {{!-- Start form --}}
        <form action="POST">

          {{!-- <div class="form-group">
            <label for="username">Test Username</label>
            <input type="text" id="username" class="form-control" aria-describedby="username" placeholder="Username">
          </div> --}}

          <div class="form-group">
            <label for="post-text">Test Post Name</label>
            <input type="text" id="post-text" class="form-control" aria-describedby="post-text"
              placeholder="Enter Your Headline">
          </div>

          <div class="form-group">
            <label for="post-description">Test Post Body</label>
            <textarea class="form-control" id="post-description" aria-describedby="post-description"
              placeholder="Enter Your Personal"></textarea>
          </div>

          <button id="submit" class="btn btn-primary float-right" data-id={{user.id}}>Submit</button>

        </form>
        {{!-- End form --}}

      </div>

    </div>

    {{!-- Posts Row --}}
    <div class="row">

      <div class="col col-12">



        {{!-- button to trigger modal with this user's bio --}}
        {{!-- <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#viewOtherBioModal"
          data-whatever="INSERT_USERNAMEHERE">Who is
          this?</button> --}}



        <div id="post-list">
          {{#each Post}}
          <div class="card mb-4">
            <div class="card-body" data-id="{{this.id}}">
              <i id="flag" class="material-icons float-right">outlined_flag</i>
              <h3 class="card-title" data-title="{{this.title}}">{{this.title}}</h3>
              <p class="card-text" data-body="{{this.body}}">{{this.body}}</p>
              <button type="button" class="btn btn-link float-right" data-toggle="modal"
                data-target="#viewOtherBioModal" data-whatever="{{this.accountId}}">{{this.accountId}}</button>
              <h6 class="card-subtitle mb-2 text-muted">Date/TimeStamp?</h6>
            </div>
          </div>
          {{/each}}
        </div>

        <div class="card mb-4">
          <div class="card-body">
            <i class="material-icons float-right">outlined_flag</i>
            <h3 class="card-title">This is the title</h3>
            <p class="card-text">If we can add a timestamp we should. If timestamp is added, need to rearrange username
              and time stamp below to be right-aligned.</p>
            <button type="button" class="btn btn-link float-right" data-toggle="modal" data-target="#viewOtherBioModal"
              data-whatever="INSERT_USERNAMEHERE">Username_of_this_Post</button>
            <h6 class="card-subtitle mb-2 text-muted">Date/TimeStamp?</h6>
          </div>
        </div>

        <div class="card mb-4">
          <div class="card-body">
            <i class="material-icons float-right">outlined_flag</i>
            <h3 class="card-title">This is the title</h3>
            <p class="card-text">If we can add a timestamp we should. If timestamp is added, need to rearrange username
              and time stamp below to be right-aligned.</p>
            <button type="button" class="btn btn-link float-right" data-toggle="modal" data-target="#viewOtherBioModal"
              data-whatever="INSERT_USERNAMEHERE">Username_of_this_Post</button>
            <h6 class="card-subtitle mb-2 text-muted">Date/TimeStamp?</h6>
          </div>
        </div>

        <div class="card mb-4">
          <div class="card-body">
            <i class="material-icons float-right">outlined_flag</i>
            <h3 class="card-title">This is the title</h3>
            <p class="card-text">If we can add a timestamp we should. If timestamp is added, need to rearrange username
              and time stamp below to be right-aligned.</p>
            <button type="button" class="btn btn-link float-right" data-toggle="modal" data-target="#viewOtherBioModal"
              data-whatever="INSERT_USERNAMEHERE">Username_of_this_Post</button>
            <h6 class="card-subtitle mb-2 text-muted">Date/TimeStamp?</h6>
          </div>
        </div>

      </div>

    </div>

  </div>

</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

<script src="/js/index.js"></script>

<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"
  integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
  integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>

<div class="row">
  <div class="modal fade" id="viewOtherBioModal" tabindex="-1" role="dialog" aria-labelledby="viewOtherBioModalLabel">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">

          <h4 class="modal-title" id="viewOtherBioModalLabel">{{this.username}}'s Bio</h4>

          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
              aria-hidden="true">&times;</span></button>

        </div>
        <div class="modal-body">
          {{!-- <p id="bioUser">@</p> --}}
          <p id="bioPro"></p>
          <p id="bioInsta"></p>
        </div>
      </div>
    </div>
  </div>
</div>